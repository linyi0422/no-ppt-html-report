# Repository Instructions

这个仓库的核心主题是“可调式 HTML 汇报页”：不用 PPT，把复盘、方案、问题说明做成可以在浏览器里调节的单页展示稿。

## Product Memory

开始任何产品体验调整前，先读 `PRODUCT_MEMORY.md`。

关键判断：

- 这不是求职仓库，也不是单个谈薪案例仓库。
- 当前谈薪复盘只是案例 01，用来证明这种展示方式。
- 产品重点是可调式汇报展示方式，而不是静态网页或图片海报。
- 以后新增 HTML 展示材料，默认加入控制面板、预设按钮、模块级布局变量、重点内容调节和纯净截图模式。

## Implementation Preferences

- 优先使用单文件 HTML：CSS 和 JS 内联，方便 GitHub Pages 发布和本地直接打开。
- 控件必须影响真实布局变量，例如字号、行距、padding、grid 占比、页面宽度。
- 不要用 `transform: scale()` 放大主要内容区，因为容易造成越界和截图不可控。
- 任何重点区域都应该能单独调整，不要只提供全局字号。
- 保留纯净模式，方便截图和公开展示。
- 页面必须先像一页汇报稿，再像一个网页。

## UX Checks

交付前至少检查：

- 文本是否越界。
- 重点区是否能单独调节。
- 控制面板是否会遮挡主体内容。
- 纯净模式是否隐藏控制面板。
- GitHub Pages 路径是否仍然可访问。

