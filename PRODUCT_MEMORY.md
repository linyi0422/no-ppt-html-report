# Product Memory: Sylan-no-ppt

这份文档记录 Sylan-no-ppt 的产品判断，方便后续在新工作区继续演进。

## 一句话定位

Sylan-no-ppt 是一组 AI-first HTML 展示产物模式，让用户和 Codex / Claude Code 完成任务或讨论后，直接得到可以展示、微调、交互、截图和发布的材料。

## 当前命名

优先使用：**Sylan-no-ppt**

说明：

- `no-ppt` 表示不复刻传统 PPT。
- 当前实际承载形式是 HTML。
- 如果后续需要更清晰表达 HTML，可再调整为带 `html` 的命名。

## 当前优先场景

优先打穿：**任务复盘**

原因：

- AI 在完成任务后天然拥有上下文。
- 任务复盘通常需要解释背景、过程、结果、问题、决策和下一步。
- 这些内容适合被整理成一页或一组 HTML 展示稿。
- 相比 PPT，HTML 更适合保留调节器、白板和交互状态。

## 当前输出形态

### Report HTML

一页式可调汇报页。

适合：

- 任务复盘
- 问题说明
- 方案对齐
- 咨询前置材料

默认能力：

- 控制面板
- 预设按钮
- 模块级布局变量
- 字号和行距调节
- 模块间距和内边距调节
- 重点区域权重调节
- 纯净截图模式

### Board HTML

白板型 HTML 展示页。

适合：

- 任务路径复盘
- 讨论结构梳理
- 流程图和决策树
- 系统关系说明

默认能力：

- 白板节点
- 节点拖动
- 节点间连线
- 纯净展示模式
- 导出 `.excalidraw`

## 参考但暂不作为主线

`frontend-slides` 是重要参考，尤其是：

- 单 HTML
- 低依赖
- show, don't tell
- 视觉预览优先
- 严格适配视口

但 Sylan-no-ppt 当前不优先做传统多页 slides。Slides 可以作为后续方案，不作为第一阶段主线。

## 关键设计原则

内容优先。页面首先是展示稿，不是普通网页，也不是营销页。

控件要改变真实布局。不要只做视觉缩放。

重点模块要能单独调节。用户经常只想调整某一块，例如结论区、问题区、白板区。

纯净模式必须保留。最终展示、截图、公开发布时需要一键隐藏控制面板。

边界和溢出控制非常重要。文字和模块不能越界、遮挡或拥挤。

## 下一步优先级

高优先级：

- GitHub Pages 入口页改成方案目录。
- 增加任务复盘 Board HTML 示例。
- 增加 Codex recipe，说明如何从任务上下文生成 no-ppt 页面。
- 增加导出 `.excalidraw`。
- 增加一键复制当前调节参数。

中优先级：

- 多套视觉主题。
- 控制面板折叠。
- 页面内编辑模块内容。
- 支持导入 Markdown。

暂不优先：

- 用户系统。
- 模板市场。
- 复杂 Web App。
- 完整替代 PPT 的所有能力。
