# Product Memory: 可调式 HTML 汇报页

这份文档用于新工作区继续微调产品体验。它记录的是产品范式，不是某个具体谈薪案例。

## 一句话定位

不用 PPT，把复盘、方案、问题说明做成一个可以在浏览器里调节的 HTML 汇报页。

核心价值不是“网页设计”，而是让一页汇报材料可以被快速生成、现场微调、截图传播、公开发布和复用。

## 暂定命名

优先使用：**可调式 HTML 汇报页**

也可以按语境使用：

- 可调式汇报展示页
- 参数化汇报稿
- 交互式问题说明页
- 可截图 HTML 汇报稿
- playground-style report demo

命名还没有最终确定。后续产品体验可以继续探索更自然的叫法。

## 解决的问题

PPT 和静态图片在快速沟通里有几个明显痛点：

- 改字号、留白、层级很慢。
- 多次迭代容易变成调格式，而不是调表达。
- 想临时突出某个模块时，经常要重排整页。
- 静态图一旦生成，后续微调成本高。
- 发给别人看时，常常需要不同版本：编辑版、截图版、公开版。

这个产品范式的目标是把“给别人看的一页说明”做成一个可调节的浏览器页面。

## 典型用户流程

1. 用户给 AI 一段原始材料：复盘、会议记录、方案、问题说明、咨询前置材料。
2. AI 先整理信息结构：背景、核心复盘、主要问题、待判断点、行动建议。
3. AI 生成单个 HTML 文件，CSS 和 JS 内联，直接可以打开。
4. 页面右侧或顶部带控制面板，可以调模块级布局。
5. 用户在浏览器里现场微调字号、行距、留白、重点区大小、问题卡大小。
6. 用户切到纯净模式，截图、分享，或发布到 GitHub Pages。

## 默认交互范式

以后凡是要做成 HTML 展示的材料，默认使用这套可调范式。

页面应至少包含：

- 控制面板
- 预设按钮
- 模块级布局变量
- 字号和行距调节
- 模块间距和内边距调节
- 重点区域权重调节
- 关键问题字号调节
- 纯净截图模式

推荐控件分组：

- 总体：页面宽度、模块间距、通用内边距、重点字力度。
- 上半区：上半区字号、上半区行距。
- 重点区：重点区字号、重点区行距、左右占比、重点区内边距。
- 问题卡：关键问题字号、问题卡内边距。
- 模式：均衡、突出问题、适合截图、纯净模式。

## 关键设计原则

内容优先。页面首先是汇报稿，不是普通网页，也不是营销页。

模块要能独立调节。不要只给一个全局字号或整体缩放。

不要用 `transform: scale()` 放大主要内容区。它会造成边界溢出和截图不可控。应使用真实参与布局的 CSS 变量，例如宽度、字号、行距、padding、grid 占比。

控制面板应影响真实布局，而不是只做视觉假缩放。

重点问题要成为视觉中心。尤其是“我想让对方帮我判断什么”这一类内容，需要有单独的问题卡。

纯净模式必须保留。用户最终大概率要截图或发给别人看，控制面板需要一键隐藏。

## 跟 AI 沟通的推荐说法

```text
我想把下面这段材料做成一个“可调式 HTML 汇报页”，不要做 PPT。

使用场景：
我要把它发给【目标对象】，希望对方快速理解【核心问题/核心结论】。

内容材料：
【粘贴原文、复盘、方案、会议记录或问题说明】

请你帮我做成一个单页 HTML，要求：
1. 页面本身就是可展示的汇报稿，不要做成普通文章。
2. 信息结构分成 3-5 个模块，每个模块有清楚标题。
3. 最重要的问题/结论要做成视觉中心。
4. 右侧或顶部加入调节面板，可以调字号、行距、模块间距、重点区域大小、关键问题字号。
5. 加一个“纯净模式”，隐藏控制面板，方便截图或公开展示。
6. 所有 CSS 和 JS 写在一个 HTML 文件里，方便直接打开和部署到 GitHub Pages。
7. 视觉风格要符合这个场景：【专业/克制/自然/科技/咨询/学术/产品汇报等】。

请先整理信息结构，再直接输出完整 HTML 文件。
```

## 现有案例

公开仓库：

- Repository: `https://github.com/linyi0422/no-ppt-html-report`
- GitHub Pages: `https://linyi0422.github.io/no-ppt-html-report/`
- Demo: `https://linyi0422.github.io/no-ppt-html-report/demos/layee-hr-retro.html`

当前案例是“谈薪复盘展示”。它只是一个案例，不代表产品只服务求职或谈薪场景。

## 已经验证过的判断

图片生成不适合这类材料的反复微调。静态图每次小改都要重新生成，且无法精确控制模块和文字。

HTML 更适合这类场景，因为它可以把“展示稿”和“调节器”放在同一个文件里。

用户真正想要的是把表达调顺，而不是只要一张好看的图。

模块级调节比全局调节重要。用户经常只想调某一块，例如问题区、最后一句判断问题、上半区背景信息。

边界和溢出控制非常重要。所有文本和卡片必须在容器内，不允许最后一部分冲出边界。

## 下一步产品体验方向

优先级较高：

- 一键导出 PNG。
- 一键导出 PDF。
- 控制面板状态持久化，例如写入 URL query 或 localStorage。
- 复制当前调节参数，方便复现同一版式。
- 案例匿名化/脱敏开关。
- 新建页面时选择模板：复盘型、方案型、问题说明型、咨询前置型。

优先级中等：

- 控制面板折叠。
- 多套视觉主题。
- 模块内容可在页面内编辑。
- 支持导入 Markdown。
- 支持把当前页面保存成新的 HTML 文件。

暂时不优先：

- 做成复杂 Web App。
- 上用户系统。
- 做过重的模板市场。
- 追求 PPT 全能力替代。

## 新工作区注意事项

先把它当成一个轻量产品体验来调，不要过早工程化。

第一目标是让用户能更快地把原始材料变成“可展示、可微调、可截图”的单页汇报。

每次改体验时都要检查：

- 控件是否真的解决微调问题。
- 重点模块是否能单独调整。
- 纯净模式是否适合截图。
- 页面是否仍然像一页汇报，而不是普通网页。
- 文字是否始终不越界、不遮挡、不拥挤。

